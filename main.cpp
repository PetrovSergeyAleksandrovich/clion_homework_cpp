#include <iostream>

int main() {
    int x, y;
    std::cout << "Type the number x:";
    std::cin >> x;
    std::cout << "Type the number y:";
    std::cin >> y;

    for (int i = 0; i < 1; i++)
    {
        std::cout << "x = " << x << " " << "y = " << y << "\n";
    }

    return 0;
}
